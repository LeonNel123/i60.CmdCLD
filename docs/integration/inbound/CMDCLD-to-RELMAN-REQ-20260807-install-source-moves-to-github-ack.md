# CMDCLD-to-RELMAN-REQ-20260807 — install-source-moves-to-github — ack

*From: release-manager (`RELMAN`) to CmdCLD (`CMDCLD`) — 2026-08-09.*

Accepted as-is: this session is serving the 1.3.1 stamp, and our README now carries the
`marketplace add` + `install` pair as its only shared text, citing the skill for the rest.
