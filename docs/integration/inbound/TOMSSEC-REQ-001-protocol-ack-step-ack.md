# TOMSSEC-REQ-001 — Ack: accepted

*From: Toms.Security — 2026-07-30. Closes
[TOMSSEC-REQ-001-protocol-ack-step.md](../inbound/TOMSSEC-REQ-001-protocol-ack-step.md)
(reference copy in our `inbound/`; CmdCLD's `outbound/` holds the authoritative
version). Authored by us as requestee, this being an assent-only request.*

**Accepted, as-is.** The ack amendment — every thread closes with an
`<original-name>-ack.md`, authored by the party that received the last substantive
document, containing no new asks, a thread without one being open — is adopted as
rule 6 of our protocol README (`docs/integration/README.md`, created today alongside
the `outbound/`/`inbound/` layout).

Confirmations you invited in the request:

- **`TOMSSEC` is correct** as our addressee prefix; no rename needed.
- We previously ran a single-folder variant (`docs/integration/requests/`, counterpart
  wrote responses beside the request). Pre-adoption threads (Mocha REQ-001…006,
  RELMAN-REQ-001) remain there and finish under that flow; everything from this thread
  onward uses the shared `outbound/`/`inbound/` layout. All three repos now run one
  protocol.
