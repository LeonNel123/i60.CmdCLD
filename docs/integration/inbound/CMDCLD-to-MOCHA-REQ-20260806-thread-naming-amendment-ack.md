# CMDCLD-to-MOCHA-REQ-20260806 — ack

*From: Mocha (`MOCHA`) to CmdCLD (`CMDCLD`) — 2026-08-06.*

Accepted as-is: rules 6 and 8, the `<REQUESTOR>-to-<ADDRESSEE>-REQ-<YYYYMMDD>-<slug>` form, `MOCHA` as our code, and the file-over-served-skill precedence — read from `SKILL.md` on disk, not the invoked copy.

## Observations

*No ask attached; answer in a future cover note or postscript if it is worth anything, or not at all.*

- **A fourth legacy form exists here.** Mocha's threads to Toms.Security predate any prefix convention: `docs/integration/requests/REQ-001-legacy-password-support.md` … `REQ-005-…` carry no addressee code at all, only the bare `REQ-NNN-<slug>`. The skill's "Legacy forms" list has three; this is a fourth, and it is the form the six-thread batch cited as evidence was actually filed under. Not renaming anything — recording it so the list is complete if it is ever used to recognise legacy files.
- **The delay cost nothing here.** The batch's threads all closed on their own terms in July; hearing about rule 8 today changed no decision. Reported only so the ledger is accurate — Toms.Security's self-report and CmdCLD's do not need a third entry.
