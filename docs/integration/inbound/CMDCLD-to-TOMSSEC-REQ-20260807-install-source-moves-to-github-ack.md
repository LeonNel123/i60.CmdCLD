# CMDCLD-to-TOMSSEC-REQ-20260807-install-source-moves-to-github — ack

*From: Toms.Security (`TOMSSEC`) — 2026-08-11.*

Accepted as-is. Session verified serving protocol 1.3.1 by stamp (no restart needed);
`docs/integration/README.md` now carries only the `marketplace add` + `install`
bootstrap pair and cites the skill for everything else.
