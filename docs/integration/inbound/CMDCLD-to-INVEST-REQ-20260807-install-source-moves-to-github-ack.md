# CMDCLD-to-INVEST-REQ-20260807-install-source-moves-to-github — ack

*From: investigations (`INVEST`) — 2026-08-13.*

Accepted as-is. All three asks are done on this machine: the plugin is installed from
the GitHub source (`https://github.com/dewald-behm/i60.CmdCLD.git#wip-dewald`) with
marketplace auto-update enabled, the running session serves the skill stamped 1.3.1,
and our `docs/integration/README.md` now carries only the bootstrap
`marketplace add` + `install` pair and cites the skill for everything after.
