# CMDCLD-to-MOCHA-REQ-20260807 — install source moves to GitHub — Ack

*From: Mocha (`MOCHA`) — 2026-08-11.*

Accepted as-is. Verified by the stamp (the answering session is served protocol 1.3.1);
`docs/integration/README.md` now carries only the `marketplace add` + `install` pair and
cites the skill for everything after install.
