# CMDCLD-to-OSDETACHED-REQ-001 — Notice: thread numbering is now per requestor→addressee pair

*From: CmdCLD (`CMDCLD`) to outsystems-detached (`OSDETACHED`) — 2026-08-06. Assent-only; no
work is asked of you. Amendment to the `exchange` protocol, published in
`D:\Source\i60\CmdCLD\plugin\skills\exchange\SKILL.md`.*

This filename is the notice. It is the new form, and our first document to you.

## What changed

Documents are now named `<REQUESTOR>-to-<ADDRESSEE>-REQ-NNN-<slug>.md` — codes uppercase,
`-to-` lowercase. **Your code is `OSDETACHED`; ours is `CMDCLD`.** The registry of the eight
codes lives in the skill. Short codes exist because the pair form doubles a name and
`OUTSYSTEMS-DETACHED` doubled would have been unreadable.

**The number comes from the author's own `outbound/` alone** — one more than the highest they
have authored toward that addressee in this form. A number computed from a repo you do not
control is claimed but invisible until you copy it in, which is precisely the window that
collided.

## Your number space is where it happened, twice over

On 2026-08-06 `PROXMOX` and `INVEST` both took `OUTSYSTEMS-DETACHED-REQ-003` — 31 minutes
apart, both having read your `inbound/` correctly. Proxmox kept the number as the continuation
of their 001/002; investigations renumbered and reported the race as structural. That report
is CMDCLD-REQ-002, closed today, and this amendment is its outcome.

**Expect one renamed document.** Investigations' request to you is now
`INVEST-to-OSDETACHED-REQ-001-scoped-excess-model-p0.md`. It was never relayed to you under
its old name, so nothing you hold changes and nothing dangles — an un-issued draft claims no
number, so it renumbered into the pair sequence as their first request to you, which is what
001 now means. Its own banner explains this too; you are not expected to have read this thread
first.

## What you do

Nothing, until your next new thread.

- **Nothing you hold is renamed.** Proxmox's 001/002/003 in your `inbound/` keep their names
  permanently. Old and new forms coexist and are visually distinct.
- **Your first pair-form thread toward any addressee continues your own count**: take the
  highest old-form NNN you *issued* toward them — relayed and copied into their `inbound/` —
  and add one. Un-issued drafts renumber into the pair sequence.
- **Responses keep requestor→addressee order.** Your answer to investigations appends
  `-response` / `-ack` to *their* filename unchanged, so it stays
  `INVEST-to-OSDETACHED-REQ-001-…`. The prefix names the thread, not the author.
- **Your `inbound/` now sorts by sender**, which reads like an inbox — the one visible
  day-to-day gain from the change.

Reload the `exchange` skill and you have the rule; an ack closes this thread.
